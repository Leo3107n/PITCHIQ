# ── Python ───────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
venv/
.venv/
backend/venv/

# ── Secrets (NEVER commit these) ─────────────────────────────────────────────
backend/.env
*.env
.env.local
.env.production

# ── Node ──────────────────────────────────────────────────────────────────────
node_modules/
dist/
.DS_Store

# ── Large ML model files (retrain locally or upload separately) ───────────────
saved_models/*.pkl

# ── Raw dataset (too large for git, 100MB+) ───────────────────────────────────
dataset/raw/male_players.csv
dataset/raw/female_players.csv

# ── Processed data (regenerated by pipeline) ─────────────────────────────────
dataset/processed/

# ── Pytest cache ─────────────────────────────────────────────────────────────
.pytest_cache/

# ── IDE ───────────────────────────────────────────────────────────────────────
.vscode/
.idea/
*.log
