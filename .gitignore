# ── Python ───────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
venv/
.venv/
backend/venv/

# ── Secrets (NEVER commit these) ─────────────────────────────────────────────
backend/.env
*.env
.env.local
.env.production

# ── Node ──────────────────────────────────────────────────────────────────────
node_modules/
dist/
.DS_Store

# ── Large ML model files ──────────────────────────────────────────────────────
# random_forest is 176MB — too large for GitHub (100MB limit)
saved_models/random_forest.pkl
# All others are committed so Render can serve predictions without retraining

# ── Raw dataset (too large for git, 100MB+) ───────────────────────────────────
dataset/raw/male_players.csv
dataset/raw/female_players.csv

# ── Processed data (regenerated by pipeline) ─────────────────────────────────
dataset/processed/

# ── Pytest cache ─────────────────────────────────────────────────────────────
.pytest_cache/

# ── IDE ───────────────────────────────────────────────────────────────────────
.vscode/
.idea/
*.log
